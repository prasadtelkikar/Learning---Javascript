#include <stdio.h>
#include <conio.h>

struct studentInfo
{
	int rollNo;
	char name[30];
	int grNo;
	float percentage;
	struct studentInfo* next;
};

struct studentInfo* CreateStudentNode();
int main()
	{
	int size;
	struct studentInfo* head = NULL;
	printf("\nEnter students present in a class: ");
	scanf("%d",&size);

	for (int i = 0; i < count; ++i)
	{
		/* code */
	}

	}